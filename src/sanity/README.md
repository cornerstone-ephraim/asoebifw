# Sanity

Sanity owns editorial content. Keep configured clients, image helpers, GROQ queries, and schemas in this boundary. Server data functions expose typed domain data to routes and features; presentational components do not query Sanity directly.
