# Features

Business capabilities are colocated here. A feature may own components, data functions, schemas, hooks, and tests when it needs them. Routes compose features; features must not import from `src/app` or from unrelated features.

Planned domains are home, designers, collections, runway, schedule, prize, after-party, and accreditation. Add each directory when its implementation begins rather than maintaining empty ceremonial folders.
