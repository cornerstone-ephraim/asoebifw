import type{Metadata}from"next";import{notFound}from"next/navigation";import{EditorialPage,EmptyNotice}from"@/components/layout/editorial-page";import{getRunwayShow,getRunwayShows}from"@/features/content/data";export async function generateStaticParams(){return(await getRunwayShows()).map(({slug})=>({slug}))}export async function generateMetadata({params}:PageProps<"/runway/[slug]">):Promise<Metadata>{const{slug}=await params;const show=await getRunwayShow(slug);return show?{title:show.title,description:"Asoebi Fashion Week runway programme."}:{}}export default async function Page({params}:PageProps<"/runway/[slug]">){const{slug}=await params;const show=await getRunwayShow(slug);if(!show)notFound();return <EditorialPage eyebrow="Runway show" title={show.title} intro="Official show information and runway film will be added when the programme is announced."><EmptyNotice title="Runway film coming soon"/></EditorialPage>}
