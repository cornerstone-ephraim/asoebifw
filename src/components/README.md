# Shared components

Feature-agnostic layout, navigation, media, typography, and primitive UI components live here. Shared components may import shared infrastructure, but must not import from `src/features` or `src/app`.
